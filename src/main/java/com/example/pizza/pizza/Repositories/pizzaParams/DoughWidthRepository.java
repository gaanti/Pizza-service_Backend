package com.example.pizza.pizza.Repositories.pizzaParams;

import com.example.pizza.pizza.domain.pizza.DoughWidth;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DoughWidthRepository extends JpaRepository<DoughWidth, Long> {
}
